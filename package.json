{
  "name": "knowledge_extractor",
  "version": "1.0.1",
  "main": "server/index.js",
  "description": "A knowledge extraction and organization platform",
  "scripts": {
    "dev": "nodemon server/index.js",
    "start": "node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-ffmpeg": "npm install ffmpeg-static --save-optional"
  },
  "keywords": [
    "knowledge",
    "extraction",
    "organization"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.52.0",
    "pg": "^8.11.0"
  },
  "optionalDependencies": {
  },
  "devDependencies": {
    "node-fetch": "^2.7.0",
    "nodemon": "^3.0.1"
  },
  "packageManager": "npm@9.0.0",
  "notes": "系统依赖：ffmpeg、yt-dlp、python3在Dockerfile中安装。仅使用openai npm包用于Whisper API调用。不使用puppeteer。",
  "overrides": {
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
